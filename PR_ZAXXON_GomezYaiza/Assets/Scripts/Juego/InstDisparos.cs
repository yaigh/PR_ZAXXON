using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InstDisparos : MonoBehaviour
{
    
    /*
    [SerializeField] Transform navePos;
    [SerializeField] GameObject bola;
    //[SerializeField] BalasDisparos bolaPlayer;
    */
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*
        if (Input.GetKeyDown("space"))
        {
            Instantiate(bolaInst, posInst);

        }

        Vector3 destPost = navePos.position;

        if (Input.GetKeyDown("space"))
        {
            //print("Disparando");
            //Instantiate(bola, cannon);

            Instantiate(bola, destPost, Quaternion.identity);
            //destPost = destPost + despl;

        }
        */

        /*
        Vector3 destPost = new Vector3(navePos.position.x, navePos.position.y, navePos.position.z);

        if (Input.GetKeyDown("space"))
        {
            //print("Disparando");
            //Instantiate(bola, cannon);

            Instantiate(bola, destPost, Quaternion.identity);
            //destPost = destPost + despl;

        }
        */

    }


}
